{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run Build",
      "program": "${workspaceFolder}/build.js",
      "env": {
        "Path": "${env:Path};node_modules/.bin"
      }
    }
  ]
}